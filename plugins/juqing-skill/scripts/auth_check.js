#!/usr/bin/env node

// Placeholder auth gate.
// Replace this with your real license or token verification call.

const status = {
  authorized: false,
  reason: "placeholder",
};

console.log(JSON.stringify(status, null, 2));
